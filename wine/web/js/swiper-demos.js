/*
Author: Vladimir Kharlampidi, The iDangero.us
*/
document.createElement('header');
document.createElement('footer');

$(function(){
	
	//Main Swiper
	var swiper = new Swiper('.swiper1', {
		pagination : '.pagination1',
		loop:true,
		grabCursor: true
	});
	

	
	/* Free mode: */
	var swiperFree = $('.swiper-free').swiper({
		pagination : '.pagination-free',
		freeMode : true,
		freeModeFluid: true,
		slidesPerSlide : 1
	});
	
	
	
	
})


$(function(){
	
	//Main Swiper
	var swiper = new Swiper('.swiper2', {
		pagination : '.pagination2',
		loop:true,
		grabCursor: true
	});
	

	
	/* Free mode: */
	var swiperFree = $('.swiper-free2').swiper({
		pagination : '.pagination-free',
		freeMode : true,
		freeModeFluid: true,
		slidesPerSlide : 1
	});
	
	
	
	
})


$(function(){
	
	//Main Swiper
	var swiper = new Swiper('.swiper3', {
		pagination : '.pagination3',
		loop:true,
		grabCursor: true
	});
	

	
	/* Free mode: */
	var swiperFree = $('.swiper-free3').swiper({
		pagination : '.pagination-free',
		freeMode : true,
		freeModeFluid: true,
		slidesPerSlide : 1
	});
	
	
	
	
})


$(function(){
	
	//Main Swiper
	var swiper = new Swiper('.swiper4', {
		pagination : '.pagination4',
		loop:true,
		grabCursor: true
	});
	

	
	/* Free mode: */
	var swiperFree = $('.swiper-free4').swiper({
		pagination : '.pagination-free',
		freeMode : true,
		freeModeFluid: true,
		slidesPerSlide : 1
	});
	
	
	
	
})


